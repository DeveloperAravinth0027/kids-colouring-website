package com.kidscolour.model.enums;

/**
 * Which product line a category is offered under.
 * BOTH means the theme is shared (e.g. "Animals" has story *and* colouring books).
 */
public enum CategoryScope {
    COLOURING,
    STORY,
    BOTH
}
